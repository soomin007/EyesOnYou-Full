# 작업 백로그

> 합의됐으나 아직 착수 전인 작업 모음. 우선순위는 사용자와 조율. **본문엔 미착수/진행중만** —
> 완료 항목은 맨 아래 '완료 이력'에 `제목 — ✅ 커밋` 한 줄로 남긴다.

## ★ 본편(EyesOnYou-Full) 다음 작업 (2026-06-24 기준 — 최우선)

본편 = 데모 3배 정식판(5막 / 한 런 ~20 / 맵 30+ / 차별점 3축: VEIL 심화·메타 진행·막별 보스).
상세 = `expansion_plan.md` ★스코프 + 메모리 `project-main-story-redesign` + `rival_veil_concept.md`.

### ★★★ 데모 상세 피드백 — 명확성/온보딩 (2026-06-26 도착, 분석=`demo_feedback_2026-06-26.md`)

> 상세 피드백 1건의 ~80%가 *명확성/온보딩*("시스템이 안 보임"). 데모/본편 공통 적용.
> - ✅ **Tier 1(즉시 수용) 전부 완료** (2026-06-26, 커밋 36f84b9~6159f09): 레버 프롬프트 · 골 "출구" 텔레그래프 ·
>   클리어 백색 소멸 연출 · EXP 바 · 스킬 티어 pip(●●○) · 저격 조준선 임박 강조 · 증기 위험 가독성 · 보상/신뢰
>   의미 병기 · 화살표 margin. 각 항목 창모드 자체검증. 상세=`demo_feedback_2026-06-26.md`.
>   - 남은 소항목: 액티브 스킬 "하나만?" / 블랙아웃 보상 표시(별도 위치 안내).
> - ✅ **Tier 2 사용자 판단 완료** (a0290d4·c46b8ce·dfbdb01): 쉴드=유지(변경X) · 글라이드 아래키 래치+툴팁 ·
>   총알 원웨이 발판 통과 · 더블점프 상승속도 안 깎이게(min). 미해결: 더블점프 "애매한 높이 항상 닿게"(발판
>   간격 튜닝, 게이트 깨짐 위험→플레이테스트 필요) · 대시 강제 맵·액티브스킬 추가(=본편 맵 작업).
> - Tier 3(해결/의도/사소): 막판선택→본편 처리선택 해결 · 코요테타임 · Up=jump · 화살표각도 동적 등.

### ★★ 데모→본편 리포커스 (2026-06-25 밤 플레이테스트 피드백 — 최우선)

> **플레이테스트 평결(사용자)**: 현재 빌드는 **데모와 체감 차이 없음.** 추가한 맵 12종은 *reskin*(같은
> 가로 통로 템플릿, 이름·적 위치만 다름 — "차 엄폐" 같은 기믹 실제로 없음). **"막"이 플레이로 안 느껴짐**
> (스테이지 번호 라벨일 뿐). 런이 짧음. 막3 흐름·9엔딩은 *속*만 바뀌고 *손맛*은 데모 그대로.
> 원인: 사용자 부재(무플레이테스트) 동안 "완주 보장 안전 패턴"만 찍어내 *다름*을 버림. 이제 테스트 가능 → 제약 해제.

**확정 방향 (사용자, 3 레버 = 결국 데모→본편 하나):**
1. **막을 '느껴지게'** — 막마다 고유 정체성(맵 성격·적/해저드 팔레트·난이도 램프) + "막 N 진입" 전환 비트.
   지금은 모든 스테이지가 "맵 골라→오른쪽으로→가끔 싸움" 같은 루프. 막이 그 루프/긴장을 바꿔야.
2. **런 길이 확대** — 막당 스테이지 ↑, 궁극적으로 **5막**(라이벌 VEIL 그릇). 9 → 훨씬 길게.
3. **'진짜 다른' 맵 소수** — reskin 12개보다 진짜 기믹 맵(수직 등반/아레나 방어/부수는 차량 엄폐/이동
   발판/해저드 주역) **적게, 제대로.**

**제약(중요)**: **3막 언저리까지는 기존 데모 느낌 유지** — 데모 유저가 익숙함을 느끼게. *다른/새로운* 체감은
그 뒤(막4~5 = 라이벌 영역)부터. reskin 12맵 처리 = **C(몇 개를 진짜 기믹 맵으로 업그레이드)**로 확정.

**▶ 1막 정체성 시범 (2026-06-26 착수)** — 막1만 작은 틀로 먼저 만들어 "이게 막이 느껴지는 거다"를 증명하고,
그 틀로 확장(reskin 재발 방지). 작게 증명 → 키우기. **설계 단일 소스 = `act_identity.md`**(막 정체성 5요소 틀
+ 막1 침투 명세). 진행:
- ✅ **설계 확정** — `act_identity.md`: 재사용 틀(성격/팔레트/난이도램프/전환비트/대비좌표) + 막1=침투(인간
  경비만, 어둠 엄폐, 전투 선택) + 막1→막2 4중 문턱 연출 명세 (커밋 dfa8303).
- ✅ **B-1 막 진입 카드** — 막의 첫 stage(0/3/6)에서 "ACT N · 이름" 비트(GameState.ACTS 단일 소스). 본편 전용
  (커밋 31c8026, 누수 수정 7f7c645 — 카드 배경 불투명·텍스트만 페이드). **실플레이 체감 OK**(사용자 확인, 연출 합격).
- ✅ **B-4 막1→막2 문턱 연출** — 4중 문턱 완성(카드+BGM전환 기존 + VEIL 진입 멘트 + 첫 드론 반응). 커밋 19a9ca7.
  `get_act_entry_line`(막2="기계도 본다"·막3="시야 안 닿는다" 예고, 밴드별) + `drone_intro_seen` 런 첫 드론
  1회 반응. 막1=인간 경비만이라 첫 드론=내부 진입 페이오프.
- ✅ **B-3 난이도 램프 + B-2 팔레트 계약** — 커밋 0539120. watchtower(막1 유일 risk3) s1→s2로 오프닝 막
  스파이크 제거(검증: s0 maxR2 / s1 maxR2·watchtower X / s2 maxR3·watchtower O, 풀 변주 유지). 막1 맵 8종
  전부 drone/bomber [] 확인 — "막1=인간 경비만" 계약 주석을 ALL_ROUTES 상단에 명문화.

> **★ 막1 정체성 시범 = 코드 완료(B-1~4).** 5요소 틀이 막1에 다 구현됨(성격·팔레트·램프·전환비트·대비좌표).

- ✅ **막2·막3 정체성** — 커밋 1bb9409. 카드(B-1)·문턱멘트(B-4)는 이미 전 막 공유 → 막2 고유작업=램프+팔레트.
  - 막2 램프: risk3 맵(substation·relay) s3 금지(min_stage 3→4) — 문턱 직후 스파이크 제거. s3 도입(r2)/
    s4 전개(ward 보장)/s5 고조(r3+blackout 보장). 검증 완료.
  - 막2 팔레트 = 기계·함정 도입(장치화 정체성). 막3 = 선형 클라이맥스(구조 완성)+시야붕괴 고유비트, 램프 무변경.
  - act_identity.md §4(막2)·§5(막3) 전체 스펙. 막별 시그니처 비트: 막2=첫 드론 반응 / 막3=시야 역전.

> **★ 막1·2·3 정체성 = 코드 완료.** 전 캠페인이 "막이 느껴지는" 골격을 가짐(카드+문턱+막별 팔레트/램프/비트).
> **남은 건 사용자 실플레이 체감 검증**(외출/무모바일로 보류). 검증되면 막4~5(라이벌) 확장 or 폴리시.

- **진짜 기믹 맵(2번 레버) 진행 중:**
  - ✅ **이동 발판 프리미티브 + '화물 리프트' 맵** — 커밋 30a6f1d. `MovingPlatform`(AnimatableBody2D 핑퐁,
    캐리 자동) + `_freight_lift`(스파이크 구덩이 위 페리 횡단, 막2 s4~5). 발판 이동·캐리·렌더 자동검증.
    완주 안전(스파이크 dmg2=즉사 아님). **타이밍 손맛은 실플레이 검증 대기.**
  - ✅ **부서지는 엄폐 프리미티브 + '차량 엄폐 통로' 맵**(2026-07-02) — `DestructibleCover`(솔리드 StaticBody,
    적탄 피격마다 HP↓·균열·파괴) + `_car_cover`(막2 s4~5, risk3). 정체성=저격 노출 통로를 부서지는 차량 뒤에
    붙어 전진. 차량이 저격 LoS 차단(숨으면 안전, 넘을 때만 노출) + 통로 끝 발사함정이 먼 쪽부터 차량을 침식
    ("머물면 깨진다"→목표 근처 점점 노출=레이스). `EnemyBullet` StaticBody 충돌 시 `hit_by_bullet` 콜백.
  - ✅ **차량 맵 fightability 수정 + 폭발 스킬=수류탄 투척 재설계**(2026-07-02, 사용자 피드백) — 엄폐가 내
    총알도 막는데 저격수를 목표 앞에 몰아둬 "숨어선 못 침"이던 문제. 폭발 스킬을 몸 폭발→**포물선 수류탄
    투척**(`Grenade.gd`, 홀드 차징 사거리+궤도/착탄/폭발반경 미리보기 `Player._draw_aim`)으로 바꿔 숨은 채 먼
    적 타격. 차량 맵 저격 3인 분산(1320/1980/2600). 총알=노출 사격, 수류탄=엄폐 사격 역할 분리. 전 맵 공통.
    **손맛/밸런스 실플레이 대기**(사거리·차징시간·각도, 저격 분산 난이도).
  - ✅ **새 실내 맵 정체성 배경 완료**(2026-07-02) — 야경 도시 대신 실내 구조(천장+기둥+형광등, env 팔레트)
    + 맵별 시그니처 소품. 주차장=구획선·주차차량 / 펌프장·응축기=배관·밸브·물 / 변전소·중계소=변압기·케이블 /
    창고·화물리프트=선반랙·상자 / 서버복도=랙·LED점멸 / 통제실복도=모니터뱅크 / 검문소=스캐너아치·스캔라인 /
    철거구역=잔해·주의테이프·먼지 / 실험구역=격자·관측유리 / 함정통로=위험스트라이프·경고등. 전 11맵 무크래시
    부팅 검증. "새 맵이 다 똑같다" 해소.
  - ✅ **강제 전진(추격) 프리미티브 + '붕괴 갱도' 맵**(2026-07-02) — `ChaseHazard`(뒤에서 210px/s 전진하는
    붕괴 벽, 700px 캡으로 상시 위협, 삼켜지면 치명) + 솔리드 잔해 장애물(`hurdles`) + `_ambience_collapse`
    (균열·지지빔·낙하잔해·먼지·붉은 비상등). 막2 s4~5 risk3. 멈추면 죽는 강제 전진 손맛(기존 신중한 전술과
    정반대). 창모드 검증(전진·치명·부팅). 배경까지 완성([[new-map-always-full-background]]).
  - ✅ **아레나 방어 프리미티브 + '반응로 제어실' 맵**(2026-07-03) — `DefenseCore`(신규) — 지켜야 할 코어 +
    침입 dome(수평 반경 360 + 지면 밴드). dome 안 지면 적 수 × drain/s 만큼 코어 HP↓, 0이면 방어 실패
    (사망 경로 재사용). **적 AI 무변경** — 잡몹은 원래 "플레이어 추격" 그대로고, 플레이어가 코어 곁(중앙)에
    서면 적이 dome로 몰려든다 → 자리를 못 비우고 양 측면을 막는 "고정 방어" 손맛(datacenter 자유 사냥
    kill-all과 정반대). 역할 분리: 근접(patrol/bomber/shield)=지면으로 들어와 코어 위협 / 원거리(발판
    저격·드론)=지면 밴드 밖이라 코어는 못 깎고 플레이어만 압박. 웨이브 3(ENEMY_CLEAR) 재사용, ARENA
    시그니처 배경(`_ambience_reactor`: 반응로 제어실). 막2 s4~5 risk3. 창모드 검증(코어 생성·밴드 판정·
    드레인·브리치·secured 전부 통과). **손맛/밸런스 실플레이 대기**(코어 hp 120·drain 6·dome 360, 웨이브 압박).
  - ✅ **쓸어내는 해저드(리듬) + '감시 회랑' 맵**(2026-07-04) — `SweepBeam`(신규) — 수직 스캔 빔이 통로를
    왼→오로 주기적으로 훑음(rest→경고→훑기 리듬). 빔이 지날 때 차폐 니치(감시 사각, x밴드 세이프존) 안에
    있으면 스캔을 피하고 노출 시 피해(dmg2). 빔 380px/s>플레이어 240 → 앞지르지 못하고 니치로 대피, rest
    동안 다음 니치로 달리는 stop-and-go 절제 리듬(collapse "계속 달려"와 정반대). checkpoint 스캐너 확장
    시그니처 배경(`_ambience_scanner`). 막2 s4~5 risk3. 창모드 검증(phase 전이·판정 5종·진입 완주 전부 통과).
    **손맛/밸런스 실플레이 대기**(빔 speed 380·rest 1.8·니치 간격 440·half 90, 리듬 난이도).
  - ✅ **부서지는 엄폐 농성(pop-and-shoot) + '저지선' 맵**(2026-07-04) — 새 프리미티브 없이 기존
    `DestructibleCover`+`BulletTrap`+웨이브 재조합. ARENA 방어(막2 s4~5 risk3): 부서지는 바리케이드 4개
    뒤에서 몸 내밀어 쏘고 숨으며(pop-and-shoot) 웨이브 3 저지. 좌우 벽 포탑(LoS 무관)이 바리케이드를 갉아
    엄폐를 **유한 자원화** → 부서질수록 노출 고조. datacenter(자유 사냥)·reactor(코어 방어)와 다른 "엄폐
    의존 농성" 손맛. 봉쇄 통제구역 시그니처 배경(`_ambience_holdout`). 창모드 검증(진입 완주·엄폐4/포탑2/
    W1스폰·부서짐 로직 통과). **손맛/밸런스 실플레이 대기**(엄폐 hp4~5·포탑 interval2.2, 적 pathing이
    엄폐 솔리드에 막히지 않는지, pop 타이밍 난이도).
  - 다음 기믹 후보(미착수): reskin 맵 일부를 기믹 맵으로 추가 업그레이드(맵 수보다 질).
    이동 발판 추가 활용 맵 / 부서지는 엄폐를 '발판'으로 변용(징검다리)도 후보.

- **다음 후보:**
  - ✅ **모바일(터치) = 폰 실플레이 가능 + 폴리시 진행 중**(2026-07-01): 터치 패드(멀티터치) + 진입 경로
    전체 터치화(튜토리얼 패드 / 오프닝·브리핑·데스·엔딩 탭) + 웹 가로 유도(`OrientationGuard`) + 터치 감지
    (maxTouchPoints) + UI 스케일(메뉴 1.4/인게임 1.1) + 버튼 배치 폴리시. **안드로이드 웹서 진입~플레이 확인**.
  - ✅ **모바일 폴리시 잔여 대부분 완료**(2026-07-02): dash/skill 버튼 미보유=흐리게+비활성·쿨다운=흐리게
    (`TouchControls._update_states`, 매 프레임 player 폴링) · ??? 방 등 `restrict_combat_input`에서 전투버튼
    (사격/대시/스킬) 숨김 · 세로 전환 시 인게임 자동 일시정지(`OrientationGuard._update_portrait_pause`, 우리
    pause만 해제) · Credits 화면 홀드=빨리감기 + Arcturus 탭 진행/닫기(emulate 마우스 좌클릭 중복 방지). 창모드
    래퍼 검증(4스크립트 컴파일·`_update_states` 동작). **남음 = 폰 실측만**: 버튼 배치·HUD 스케일 값 튜닝 ·
    세로 자동전환·자동 일시정지 실동작 · iOS(orientation lock 미지원) · 햅틱. 상세=`session_logs/2026-07-02.md`.
  - **런 길이 확대(→5막)** — 막3 엔드게임을 막5로 이주하는 라이벌 재구조화와 얽힘(아래 ★와 함께).
  - **★ 막4~5 라이벌 VEIL** — `rival_veil_concept.md`(5막 확장 전제, 막3 재구조화). 가장 큰 단위.
    - ✅ **재머 유닛(§5 1단, 라이벌 첫 간섭)**(2026-07-06, 실플레이 반복 튜닝 → 사용자 OK) —
      `EnemyType.JAMMER` 고정 방출 장치(server_hall 막3 s6). **플레이어가 반경(340) 안에 들면 = VEIL 없는
      날것 시야**: 기존 붕괴 비네트 재사용(어둠+시야 축소, 라이벌 바이올렛, **캐릭터 중심·종횡비 보정 원형**) +
      반경 안 적 마커 제거(`VeilSight._enemy_in_jam`) + 첫 진입 VEIL 반응 1회. 재머 자신은 마커로 안 잡힘
      (맹점 §1), 밝은 본체로 직접 보임. 부수면 시야 복구. 튜닝값: 붕괴 near 0.24·어둠 0.92·중심 오프셋 -24.
    - ✅ **거짓 렌더 §4 첫 타입: 위장한 적(적 종류 둔갑)**(2026-07-06) — `Enemy.disguise_as`: 참 종류
      (**방패병** HP3)의 hp/행동 유지, 시각만 정찰병으로 위장. `_GlitchTell`(붉은 전신 글리치, 원거리 가독) tell +
      **교전(피격/막힘) 시 리빌**(정면 사격이 방패에 튕기며 정체 노출 — 1방에 안 죽어 위장이 손해로 이어짐).
      MapData `deceits`(control_corridor 막3). §4.1 공정 규칙 준수. 창모드 검증.
    - ✅ **테스트 편의**(2026-07-06): 무적 모드(연습장 패널 토글, `playground_active`에서만 효과) · 에디터 실행 시
      디버그 자동 해제(`OS.has_feature("editor")`) · 연습장 진입 맵 lab→server_hall(보스 경보음 우회) ·
      업데이트 내역(PATCH_NOTES) 07-02/04/06 갱신 · 배포 워크플로 `--import` 선행.
    - ✅ **ⓐ 막3 확산 — datacenter 재머**(2026-07-07, 커밋 a12cc09) — 막3 s6 전투 풀 3맵이 모두 라이벌 요소를
      가짐(server_hall=재머 / control_corridor=위장 / **datacenter=재머**). datacenter는 ARENA(FIXED)라 통과형
      복도와 다른 새 쓰임새: 최종 웨이브(근접)에 재머 1기(1080,790) → 우측 클러스터(shield·bomber) 마커 소등
      + 시야 붕괴, `enemy` 그룹이라 ENEMY_CLEAR = 반드시 처치("우선 표적" 강제). 좌측 bomber@600은 반경(340)
      밖 유지(비대칭 안전지대 = hard but fair). 맵당 1개 유지(§4.1 남발 금지). 데이터만(코드 무변경), 창모드
      하니스 검증(최종 웨이브 재머 스폰·enemy 그룹·HP3·필드·지면 안착). **실플레이 밸런스 대기**(절정 근접+블라인드
      난이도, 재머 위치/반경). (위장 확산은 보류 — datacenter는 waves 모드라 `_spawn_deceits` 스킵, 코드 필요.)
    - ✅ **ⓒ §4 거짓 렌더 2번째 타입: 위장 함정(가시→안전 바닥)**(2026-07-07) — `DisguisedSpike.gd`(신규):
      진짜 가시(_build_spike 재사용)의 **시각만 숨기고**(안전한 바닥처럼) 데미지 zone은 항상 활성(하드 페널티).
      `_FloorGlitch`(Enemy._GlitchTell의 가로 밴드 변형) = 항상 켜진 붉은 지직거림 tell(원거리 가독) + 근접
      (150+신뢰warm 60) 또는 **밟는 순간** 리빌(진짜 가시 노출+언마스크 플래시). MapData `deceit_spikes`
      (server_hall @2150, dmg2). §4.1 공정 준수(항상 tell / 신뢰가 리빌 반경 / 맵당 ≤1 / 무시하면 실제 피해).
      Stage `_build_disguised_spikes`+`_on_spike_touched` 리빌 연결. 창모드 검증(시각 17개 숨김·zone dmg2 활성·
      리빌 시 노출·tell 제거). **실플레이 대기**(tell 톤/빈도, 리빌 반경, dmg2 밸런스, 위치 발견성).
    - ✅ **ⓓ §7 SENTINEL reveal 비트**(2026-07-07) — 막3 페이크 보스 처치 후 "이긴 순간을 이긴 것 같지 않게":
      승리 챔 억제(정적) → **라이벌 첫 발화**(`_show_rival_subtitle`, 바이올렛+화자 불명 "?", 시안 내 VEIL과 구별)
      → **내 VEIL 동요**("저는 못 봤어요", §1 맹점 실연). `_play_sentinel_reveal`(정적1.2s→라이벌→VEIL, 완료까지
      회수 게이트+소프트락 상한). **복선** = 보스전 중 가끔(7~12s) 거짓-렌더와 같은 붉은-바이올렛 지직거림
      (`_boss_glitch_flash`, modulate 무관 오버레이). 창모드 검증(라이벌·VEIL 자막 노출·완료 플래그·복선 무크래시).
      **대사=플레이스홀더**(§7 예시, 사용자 재작성). **실플레이 대기**(reveal 타이밍 ~8.5s, 톤, 복선 빈도).
    - ✅ **§4 거짓 렌더 3번째 타입: 시선 거짓(딴 데 보는 척 기습)**(2026-07-07, 커밋 51b2969) — `Enemy.feign_ambush`:
      patrol이 각성을 숨긴 채 정지·플레이어 반대로 응시(안전해 보임)하다 근접(170) 시 홱 돌아 기습(압축
      텔레그래프→돌진). `_GlitchTell` 공유 tell(항상 경고, 원거리 가독). MapData `feigns`(control_corridor 회랑
      초입 @900, 위장 적@2650과 1750px 이격 — 회랑=라이벌이 적을 가지고 노는 구간). 위장과 배타. 창모드 검증
      (정지·glitch·ROAMING → 170 접근 시 각성·glitch 제거·TELEGRAPH). **실플레이 대기**(각성 반경/압축 tele 난이도).
    - ✅ **A = 5막 재구조화 골격**(2026-07-07, 커밋 53516ec) — 3막(9)→**5막(15스테이지)**. ACTS 5막(침투/잠입/
      핵심부/추적/대면) · TOTAL_STAGES 9→15 · RUN_VERSION 3→4 · 전 맵 min/max_stage 재분배(기믹 B그룹→막4/5,
      막3 전투 s6-7, lab→s8 배타, escape→s14 배타). 신규 `route_core_recovery`(s13 배타, 막5 회수+처리선택 —
      미래 라이벌 보스 자리). **엔드게임 분리(§8)**: 처리선택을 lab(막3 SENTINEL)서 떼어 막5 회수로(SENTINEL
      처치→§7 reveal→막4 계속). VeilDialogue 브리핑 9→15칸. 창모드 검증(전 스테이지 ≥1 라우트/배타/act 매핑/
      엔드게임 분리/부팅 스모크 전부 통과, 시야붕괴 onset=막5 자동). **실플레이 대기**(15스테이지 완주·페이싱·엔딩 도달).
    - ✅ **막4/5 팔레트 분화 + core_recovery 시그니처 배경**(2026-07-08) — act_identity §6(막4 추적)·§7(막5
      대면) 5요소 설계 + 코드: ① **램프** — 막4 풀 재분배(freight 9-10 도입 / scanner 9-11 / relay·car 10-12 /
      collapse·holdout 11-12 고조 / **core_defense s12 전용** = "코어 방어→코어 회수" 서사 랠리). 300런 시뮬
      전 스테이지 풀 ≥1(s9=2·s10-12=3) + 램프 계약 검증. ② **재머 흔해짐**(§6) — relay(후반 클러스터)·
      scanner(니치 3100 오염)·holdout·core_defense(FINAL WAVE, dome 밖) 4맵에 확산, 맵당 1기 유지.
      car_cover(저격 레이스)·collapse(강제 전진)는 블라인드 unfair라 금지 명시. ③ **바이올렛 침식** —
      막4부터 CanvasModulate 캐스트(막4 옅게 1,0.94,1 / 막5 0.99,0.88,1) + 가끔 화면 간섭 밴드(막4 22-34s /
      막5 14-24s, 거짓-렌더 tell과 색·범위로 구별). 막1~3 깨끗 검증. ④ **core_recovery 시그니처 배경** —
      심장부 수렴(격벽 아치 죄어듦 + 코어로 흐르는 시안/바이올렛 데이터 펄스 + 코어 글로우 맥동), 스카이라인
      스킵. 스크린샷 확인. **실플레이 대기**(캐스트 농도·간섭 빈도·재머 4곳 밸런스·s12 core_defense 고정 체감).
    - **▶ 다음(라이벌 마무리)**: **★ 최종 보스 스테이지 = 2막 구성 — 설계 확정(2026-08-12 회의, 컨셉
      §7.1·§7.2)**: 14-2 = **2.5D 유사 1인칭 확정**(비트는 렌더러와 분리, 안 되면 소실점 2D 후퇴) ·
      14-1 = **3페이즈 + 가짜 클리어**(P1 군대 지휘 → P2 시설 빙의 → 가짜 클리어 페이크아웃 → P3 거짓
      VEIL 분신전[trust가 전투력]) · 라이벌 말투 A(결이 어긋난 정중함) 확정 · VEIL 어투 = 전투 콜아웃만
      보고체 확정. 구현 순서 제안: ① 14-2 터널 프로토(손맛 검증 먼저) ② 14-1 P1~P2 ③ 가짜 클리어+P3
      ④ 회수 문서 오버레이 리디자인(14-2 전용 연출) ⑤ 대사·BGM(14-2 무음+환경음 후보). ·
      ⓑ §5 2단 재밍 맵(막4에 제자리) · §4 상충 추천(route 좌/우) · ⓔ 실플레이 튜닝(재머·위장·함정·reveal·feign·15스테이지
      페이싱) · **B 대사 재작성(사용자)**: 막4/5 브리핑·진입멘트·라이벌/reveal 끼어듦(모두 플레이스홀더).
      - ✅ **④ 14-2 실런 배선 + 회수 문서 리디자인**(2026-08-12) — 14-1 클리어 →
        `core_tunnel_live`로 터널 직결(맵 선택 없음, §7.1). 목격 비트에서 **회수 리드아웃**(종이
        오버레이 대신 코어 곁 터미널 타이핑, 14-2 전용 연출) → VEIL 고백 → 처리 선택 → 탈출
        브리핑. 문서·고백 대사 = `VeilDialogue` 단일 소스화(스토리 lab 종이 경로와 공유).
        실런 중 ESC/P 이탈 차단(프로토 전용). 하니스 PASS(리드아웃 5줄·오버레이·플래그) +
        스크린샷 2장. **이로써 §7.1~7.2 전체 골격(14-1 3페이즈+가짜 클리어 → 14-2 터널 → 처리
        선택 → 탈출) 코드 완성 — 남은 것 = 실플레이 튜닝 + ⑤ 대사·BGM.**
      - ✅ **③ 가짜 클리어 + P3 분신전**(2026-08-12) — P2 종료 → **가짜 클리어**(진짜 문법 재현:
        챔+검은 페이드+STAGE CLEAR 문구 → 바이올렛 찢김, 전 구간 타이머 체인 = 소프트락 안전판,
        goal_reached 미설정) → **P3 = FalseVeil**(`FalseVeil.gd` 신규): 유기적 눈 본체(시선 지연 추적·
        완만 깜빡임·그로테스크 배제), 사이클 잠복 7s(무적·가짜 마커 4개 볼리+무표시 엘리트 2기 투입,
        누적 상한 3) → 응시 0.7s(동공 수축 예고) → 실체 3.4s(피격 가능·조준 3연탄). 가짜 마커 =
        구형 렌더 문법(굵은 브래킷·스캔라인·1.6Hz 커서) = 공정 tell. 무표시 위협 = `no_marker` 메타
        (VeilSight 두 루프 생략). **신뢰=지각 보조**: warm 6s/thaw 10s/cold 없음 간격으로 내 VEIL이
        가짜 하나씩 시안 취소선 소거+콜아웃. hp6 소진 → 눈 감김 소멸 → 퇴장 대사 → 진짜 클리어.
        P3 체크포인트(reached 2, 재시도 시 가짜 클리어 생략). 하니스 전 구간 PASS + 스크린샷 2장.
        대사 7줄 플레이스홀더. **실플레이 대기**(사이클 타이밍·3연탄 압박·마커 혼동 재미·assist 빈도).
      - ✅ **② 14-1 P1~P2**(2026-08-12) — route_core_recovery를 보스 아레나(1920 FIXED)로 재구성.
        P1 지휘 = 선언적 웨이브 2파 전원 엘리트(elite_chance 1.0 세트피스, cap 무시) + 재밍 노드
        좌우 그늘. P2 빙의 = 소등(CanvasModulate) + 벽 포탑 2기 + 위장 함정 + 제어 노드 2기(재머
        재사용, 측면 발판) — 전파괴 시 방 해방 → 기존 회수 흐름. 페이즈 체크포인트
        (`rival_phase_reached`, 사망 시 P2부터). 창모드 하니스 전 구간 PASS + P2 스크린샷.
        대사 5줄 플레이스홀더(말투 A·보고체). **실플레이 밸런스 대기.** 다음 = ③ 가짜 클리어+P3.
      - ✅ **① 14-2 터널 프로토**(2026-08-12) — `CoreTunnel.gd`+`core_tunnel.tscn`: 유사 1인칭 원근 렌더러
        (`_TunnelView`, 비트와 분리 · 소실점 2D로 교체 가능) + 비트 컨트롤러(걷기→순차 점등[전방 820
        트리거 + 0.5s 캐스케이드]→코어 목격[z4040 강제 정지+줌]→종료 패널). 무음+환경음(발소리 · 점등
        텅=hatch_open 대타 · 목격 스팅=arcturus_enter 대타), 헤드밥+점등 시야 충격, 터치=화면 절반 홀드.
        진입 = 연습장 "14-2 터널 프로토" 버튼. 종료 패널에서 처리 선택 미리보기(DisposalChoiceOverlay,
        선택 저장 안 함). 창모드 하니스 검증(점등 7개 순차·간격·목격 정지·패널 생성) + 스크린샷 4장.
        **실플레이 1차 합격**(2026-08-12 사용자: "생각보다 잘 만들었네") + 튜닝 반영: 전진 ↑/W ·
        터널 -20%(목격 3240, 전등 6개) · 점등음 자체 제작 light_clunk · 발소리 +3dB. 남은 확인:
        light_clunk 질감(불만족 시 ElevenLabs 교체) · 코어 도상 추후 교체.

- **✅ 디버그/이스터에그 묶음(2026-07-04)** — 상세=`session_logs/2026-07-04.md`:
  - ✅ 연습장 새 맵 누락 수정(RouteData 파생 29맵) · ✅ 디버그 표시등(DebugOverlay 우상단 칩).
  - ✅ 이스터에그 4종: 황금 희귀 적(shiny) · 평화주의 반응 · 코나미 스킨(폰=황금3처치) · server_hall 숨은 로어 문서.
  - ✅ **스킨 on/off 설정 토글**(2026-08-12) — 황금 3처치 자동 해금이 데스크톱에도 적용돼 "시작부터
    파랗다" 혼란(사용자 보고). `alt_skin_enabled`(영속, 기본 켜짐) + 설정 그래픽 탭 "숨겨진 색" 토글
    (해금 시에만 노출 — 비해금자 스포일러 차단). 해금 플래그는 유지.
  - **다음(이 묶음 잔여, 사용자 영역/실플레이)**: server_hall 로그 **문구 검토**(B⑥) ·
    실플레이 튜닝(shiny 1.5% 빈도·오라, 평화주의 대사 타이밍, 시안 톤, 로그 레버 발견성).

> **★ 다음 세션 시작점(2026-08-11 세션 3 종료)**: 3차 피드백 8건 추가 반영 — ① VEIL 특별/상황 멘트
> (황금·엘리트 첫 조우, 방패 폭발 무효 목격 — 각 런당 1회, 문구=dialogue_review §8 검토 대상) +
> 포탑 "못 부숴요" 멘트 런당 2회 제한 ② ESC/전체화면: P 보조 일시정지 키(런타임 상시 보강) +
> auto_fullscreen이 ESC·pause 중엔 재진입 안 함 ③ 배수로 "출구 위 오브" = 버그 아님(클리어 시
> 미수집 XP 환급) → 회수 궤적 라인으로 인과 시각화 ④ 드론 산개/각 잡기(측면 대기 + LoS 차단 시
> 투하 보류 — "지붕 밑 수류탄 농사" 해소, 스토리 모드는 드론 스폰 스킵이라 무관) ⑤ 엘리트 HP 상향
> (6/3/3/7, 설계안 표 갱신) ⑥ 방패 폭발 무효 이펙트 강화(확산 링+무효 라벨) ⑦ XP 곡선 2차 상향
> (8+레벨×3/4) ⑧ 자막 화자 칩 분리(VEIL·? 이름을 색/구분선으로) + 사격·대시 바 축소(44px) +
> shiny 드론 오라 중심 정렬. dash·이중점프는 이미 STARTING_SKILLS 기본 지급(트리 표시만 "(기본)")
> — 변경 없음. **전부 실플레이 대기.** 이하 세션 2 기록:
> 15스테이지 실플레이 2차 피드백 9건 전부 반영 —
> ① 저지선 근본 수정: **사냥 모드**(`waves_hunt`, 감지 밖 전진 + 엄폐 타넘기 `_try_hunt_hop`) +
> 증원 예고 텔레그래프(셰브론+차단문 광선+개방음, 조기 클리어 가드) + 저격 발판 도달 계단(마우스
> 조준 없음 → ENEMY_CLEAR 도달 보장). 반응로(core_defense)도 동형 잠복이라 함께 waves_hunt.
> ② 보스 SENTINEL 라벨을 바 왼쪽으로(스킬 행 겹침 해소) ③ shiny 금빛 강화(가산 글린트+틴트+오라)
> ④ 회수 맵 펄스 배경화(천장만·저알파 스트릭) ⑤ XP 곡선 8+레벨÷2 + 보스 HP 캡 16(만렙 보스전
> 싱거움) ⑥ 오버플로 3단(장갑→응급 처치→점수 폴백) + **HUD 점수 상시 표시**(우상단 VEIL 눈 아래)
> ⑦ 연습장 "엘리트 강제" 토글. 창모드 하니스+스크린샷 검증 완료. **밸런스 실플레이 대기**(저지선
> 농성 난이도[적이 진짜 밀려옴+타넘음], XP 감속 체감, 보스전 길이). 다음 구현 목표 변함없음 =
> **★ 최종 보스 스테이지 2막 구성(컨셉 §7.1)**: 14-1 라이벌 보스 →
> 14-2 캄캄한 복도, 14-2 구조(2.5D 유사 1인칭 vs 소실점 2D)는 사용자와 결정부터. 사용자 영역:
> **대사 전수 검토 = `dialogue_review.md`**(수정안 기입식 + **라이벌 말투 결정란**: 존댓말 추천 vs 반말)
> · **엘리트 첫 조우 VEIL 대사**("다른 신호" 복선) · 탈출 s14 BGM 분리 여부.

> 버그픽스 완료(2026-06-25 밤): 맵 선택 진행바 글씨 깨짐(box-drawing 두부)·진실 엔딩 정적표시 모순·
> 첫 맵(s0) 방패병/저격 제외. (커밋 ac35cae)

- 완료: ✅ A1 막 시스템 1급화(3막/8스테이지) · ✅ A2 첫 맵 server_hall(서버 회랑, 한 런 9).
- 완료(2026-06-25): ✅ **B1 막3 흐름 재배치**(전투 s6→핵심부 lab s7→탈출 escape s8) · ✅ **B2 회수 연출
  + 처리 선택 4지선다**(DisposalChoiceOverlay, GameState.disposal_choice) · ✅ **B3 엔딩 9개**(처리 4 ×
  신뢰 2 + 진실 1, EndingResolver 재설계). 코드·구조 완성, **대사는 전부 플레이스홀더**. 상세=메모리
  `project-main-story-redesign`.
- **다음 (착수 대기):**
  - **B 대사 검토/재작성**(사용자 영역): ① lab 회수 문서·reveal ② 처리 선택 오버레이 멘트 ③ 엔딩 9개 본문
    ④ 에필로그 9개 ⑤ 막3 인게임 자막 ⑥ **server_hall 숨은 서버 로그**(이스터에그, 라이벌 VEIL 복선 초안,
    2026-07-04 추가 — `Stage._server_log_doc_lines`). (에필로그는 ✅ 엔딩별 1챕터로 분리됨 — 문구만 검토.)
    - ✅ **③④+② 2차 초안(2026-08-12)** — 사용자 어투 지적 3회차("영화·소설처럼 자연스럽게")로 엔딩
      9종·에필로그 9종·처리 선택 VEIL 한 줄 전면 리라이트. 원칙 = dialogue_review 규약 "서사 대사
      자연스러움 원칙"(종결어미 다양화·감정 직진술 금지·행동 중계 금지·"..." 절제·한 줄 자립·hi/lo
      온도 대비) + 자동 메모리 영구화. **문구 최종 확정은 여전히 전수 검토(사용자)에서.**
    - ✅ ⑥ 2차 초안(2026-08-12) — 14장 확정 설정 직조(위 라이벌 항목 참조).
    → **단문류 전수 검토 문서 = `dialogue_review.md`**(2026-08-10, 수정안 기입식 — 보스 인트로·reveal·
    막 진입/브리핑·회수 문서·처리 선택 포함. 장문 ③④⑥은 포인터만, 요청 시 추출).
  - **A2 진행**: ✅ 신규 맵 3종(주차장 막1 / 변전소·실험구역 막2, 기존 풀 합류 — 맵 16개). **런 길이 확대
    (한 런 ~18-20)는 보류** — stage 수 증설은 페이싱·난이도 검증(플레이테스트) 필요. 추가 맵 양산(30+)은 가능.
  - **A3 신규 적**(expansion_plan): ✅ 재머(VEIL 마커 교란, 2026-07-06 — 위 라이벌 VEIL 참조) ·
    엘리트(**설계 확정 2026-08-10** = `elite_enemies_plan.md`, 구현 대기) — 밸런스 검증 필요.
  - **★ 후반 컨셉 = 적대적 라이벌 VEIL**(2026-06-25 회의 확정, 미구현): 시설 안 두 번째 VEIL이 3막부터
    슬쩍 간섭 → 5막 대놓고. 시야(렌더) 강탈로 거짓을 보여주고, 쌓은 신뢰가 진짜/가짜 구분 능력이 됨.
    SENTINEL=3막 페이크 보스. 막3 회수/처리/엔딩은 5막으로 이주. 상세·열린질문 = `rival_veil_concept.md`.
    **5막 확장 전제** — 착수 전 라이벌 동기 등 열린질문 닫기.

---

## 0. 방향 전환 (2026-06-23) — 부스 종료 → 웹 개인 플레이 / EA 데모 → 본편

> **과제전 부스 전시는 끝났다.** 이제 설계 기준은 **"웹 환경에서 각자(개인) 진행"**. 부스/QR/키오스크
> 가정(매 타이틀 진입=새 세션, 도감 리셋 등)은 더는 기준이 아니다. **현재 빌드 = 얼리 액세스/데모 느낌으로
> 마감**(다회차·리플레이 포함), **그다음 본편을 실제 게임처럼 분량 확 늘려 제작**.

- **본편 확장(분량 대폭 ↑)**: 스테이지/맵/적/스토리/엔딩을 정식판 규모로 확대. 현재 = EA 데모. *다음 큰 단위.*
  → **제안/전략 단일 소스 = [`expansion_plan.md`](expansion_plan.md)** (아트가 절차적 코드라 확장은 코드·설계,
  병목은 BGM뿐. Phase A 분량·다양성[에셋 0] → B 스토리 깊이 → C 메타/스킨 → D 오디오). 착수 시 막 경계·맵 풀·
  적/스킬 목록부터 확정.
- **웹 개인 플레이 전환**: (1차 ✅ 적용) 부스 도감 리셋(`Title._ready` `seen_enemies.clear()`) 제거 →
  도감·본 엔딩(`endings_seen`)·완주 카운트(`playthrough_count`) 영속(settings.cfg). **단일 자동저장
  이어하기** — `user://run.cfg`에 RouteMap 진입마다 자동저장, 타이틀 "이어하기"로 RouteMap 복귀
  (`save_run`/`load_run`/`has_run`/`clear_run`). 엔딩 도달 시 메타 적립 + run 삭제, 새 게임 시 run 삭제.
  **실측 현황**: 영속은 **GitHub Pages(github.io)에서 사용자 확인 완료**(브라우저 완전 종료 후 스킬·레벨·
  도감 유지). 메인 호스트 = github.io. 웹에선 종료 버튼 숨김(브라우저가 탭을 못 닫음). **남은 점검**: 웹 전용
  이슈(오디오 autoplay 정책·입력·캔버스 크기·성능) 정도. 다회차 리플레이 대사 차별화(아래)는 별도 —
  이제 `playthrough_count`/`endings_seen`이 신호로 준비됨.
- **VEIL 시야 붕괴 onset**: (데모 ✅ 적용) 보스/탈출 직전 첫 전투 맵(일반 stage>=4 / 스토리 stage2~3)을
  **"진입부터 붕괴"**로 — `record_route_choice`가 onset에서 `veil_degraded`+`veil_reversal_pending`을 켜
  VeilSight가 시작부터 어둡고, 진입 시 역전 멘트 1회(`Stage._setup_veil_mistakes`). 중간 글리치·자막 겹침
  ("갑자기 와다닥") 제거(사용자 보고). 보스 ARENA·탈출은 상속/제외. **본편 과제(남음)**: 확장 스테이지
  구조에 맞춰 역전 비트 위치·강도 정식 재설계(현재 stage>=4 첫 발동은 데모용 임시 정렬).
- **인게임 대사 register/붕괴 인식**(본편 대사 패스, 사용자 보고 2026-06-23): `register_band`(딱딱→따뜻)와
  시야 붕괴 상태가 **브리핑·사망·위협콜아웃에만** 반영되고, **entry_comment·보스 경고·ACT3 자막·스킬 멘트는
  해요체 하드코딩**이라 초반 COLD 구간·붕괴 상황과 톤이 어긋남(예: 보스전에서 "못 봐요" 상태인데 경고는
  자신만만한 해요체 전술 콜아웃). 인게임 자막을 밴드/붕괴 인식하게 정식 재설계(대사 다수 재작성 + COLD
  변형 작성). 어투 결정은 사용자 영역 — 본편 작업 단위로.

## A. 플레이테스트로만 확인 가능 (내가 못 함 — 사용자 플레이 필요)

- **피드백 설문 응답 수집** → 1차 응답 3건 도착(06-19·06-23 설문 + 교사반 카톡). 명확성(최종목적·보스안내·
  회피정당화) + 보스/드론 완화 반영 완료(2026-06-23). 추가 응답으로 체감 튜닝 지속. ([[feedback-form]])
- **내보낸 빌드 체감**: 해상도 실제 적용·텍스트 선명도·비네트 농도(0.10)·맵 간 어두움 지속. export 설정 도움 가능.
- **VEIL 추천 임계값 튜닝**: `PERF_SKILLED_HITS_MAX=1` / `PERF_STRUGGLE_HITS_MIN=4`(최근 2스테이지 평균)가 체감과 맞는지.
- **신뢰 단계 튜닝**: trust 임계(0/4/8/12), 수용률 0.5, 게이지 스케일[2,4,8,12,16], 취약함 게이트 체감(무사망→COLD가 너무 차갑진 않은지).
- **스킬 외형 점검**: 전투 중 총기 회전·부착물 겹침·가독성, 획득/티어업 플래시 체감, 활강 유도↔사격강화 관통 분리 체감.
- **함정/저격수 난이도**: 후반 트랩(datacenter·watchtower), 둥지 저격수 LoS·사선 노출, 글라이드 게이트 도달 난이도.
- **shield T3 재충전**: 재충전 시간(30s)·재무장 플래시 톤.
- **포스터 2배 고해상 인쇄 가독성** + 필요 시 PNG→PDF 외부 변환.

## B. 콘텐츠 / 게임플레이 (방향 결정 필요)

- ~~**오디오 보강(BGM·효과음)**~~ — **보류/종결**(2026-06-23 사용자: "정확히 뭐가 문제인지 모르겠으니
  해결된 걸로 치고 넘어가자"). 추후 본편 오디오 폴리시(expansion_plan Phase D)에서 필요 시 재개.
- **콘텐츠 확장(스킨·스토리)**: 설문2 "결말 A를 봤는데 다른 엔딩 모으러 다시 하고 싶다" + 추가 희망
  "캐릭터 꾸미기(스킨)·더 깊은 스토리 확장". 리플레이 동력은 확인됨 — 방향·범위 결정 필요(이번 제외).
- **다회차 차별화 — 리플레이 오프닝/VEIL 대사**(사용자 요청 2026-06-23): (오프닝 ✅ 적용·초안) 완주 1회 이상
  (`playthrough_count>=1` 또는 `replaying`)이면 오프닝(`INTRO_SYSTEM_REPLAY`/`INTRO_VEIL_REPLAY`)이 변형 —
  작전명 PALIMPSEST(덮어쓰여도 흔적) + 루프/리셋 테마로 VEIL이 "기록상 처음인데 낯익다". 4번째 벽 금지
  유지(플레이어 직접 호명 X, VEIL의 흔적/기억으로). **추가 적용**: 엔딩 수집 비트(`endings_seen` 3/4=남은
  갈래 암시·4/4=완수 인정), ??? 방 첫 단말기 변형 신호를 `replaying or hidden_visit_count>=1`로 보강(이 방을
  본 적 기준 — 다회차여도 처음 찾은 사람은 VEIL-1 reveal 보존). **남음**: 중반 VEIL 대사 변형 후보를
  `docs/design/veil_replay_dialogue.md`에 정리(비트별 1회차/다회차 후보 + 배선 메모) → **사용자 검토 후
  채택분 배선**.

- **글라이드 유리한 맵**: 저격병을 글라이드로만 닿거나 회피하는 위치(고지대 단독 발판 / 긴 갭 너머 / 긴 낙하 구간)에 둔 맵.
  신규 맵 vs 기존(rooftops/watchtower) 개조 — 방향 미정.
  - `map_audit.md` 결과: 빈칸은 "유리 지형 부재"가 아니라 ① **회피 강제 저격수**(못 잡고 지나가야 하는 위치)
    ② **유인 강화**(글라이드/대시로만 닿는 지름길·보상).
- **유도 차지 사격 스킬**(신규 아이디어): 공격을 꾹 누르면 연사 대신 누른 시간만큼 유도되는 강탄. 매력적이나
  차지 시스템 신규 + multishot T3 / glide T3 유도와 역할 중복 검토 필요.
- **공격 속도 라인 부재**: 전용 공속 패시브 없음(fire_boost T2의 짧은 가속뿐). 위 차지 스킬로 대체할지 검토.

## C. 코드 정리 (착수 시 플레이테스트 동반)

- **Stage.gd 분리** — 3580줄 god-object, 수십 개 공유 멤버로 엮임. 자율 보류. 단계적 안전 계획:
  ① 가장 독립적인 `_ambience_*`(12개, 순수 장식)를 `StageAmbience` 헬퍼로 추출 → ② HUD 빌드/갱신 묶음
  → ③ 적 스폰/웨이브. 각 단계 후 전 스테이지 타입 플레이 확인. 사용자가 함께 검증할 때 착수 권장.
- **Pretendard-Medium 도입**: 외부 폰트 자산 필요. 전역 아웃라인 테마로 "중간 굵기" 체감 확보돼 우선순위 낮음.

## D. 문서 / 정리

- **README/INDEX 점검**: 완료/스테일 항목 정리, "최근 시스템"에 이번 UI·포스터·피드백 작업 반영(필요 시).
- **STORY.md §2(맵 표)** 등 남은 스테일분 점검.

---

## 완료 이력 (한 줄)

- 3차 피드백 배치: VEIL 특별/상황 멘트 3종 · ESC/전체화면(P 보조 키) · 오브 환급 궤적 · 드론 산개/LoS · 엘리트 HP 상향 · 폭발 무효 이펙트 강화 · XP 곡선 2차 · 자막 화자 칩 · 게이지 축소 — ✅ (2026-08-11 세션 3)
- 실플레이 2차 피드백 배치: 저지선 사냥 모드+증원 텔레그래프+저격 계단 · 보스 라벨 겹침 · shiny 금빛 · 회수 펄스 · XP 곡선+보스 캡 · 오버플로 응급 처치+HUD 점수 · 연습장 엘리트 강제 — ✅ (2026-08-11 세션 2)
- 엘리트(라이벌의 군대) 구현: 막4/5 확률 승격 + 바이올렛 계급장 + 타입별 템포 강화 + 방패병 폭발 면역 + 도감/잠금 — ✅ 694dd37 (2026-08-11)
- 회수 시퀀스 분리(문서→고백→처리 선택) + 보스 인트로/reveal 대사 명확화 — ✅ 49a97f3 (2026-08-10)
- 막4/5 신곡 배선(Broken Wire·Violet Signal, 막 기반 선곡) + 보스 인트로 컷씬 + 보스 HP 스케일 + XP 곡선 — ✅ 2b744b5 (2026-08-10)
- 붕괴 벽 캡 추격 스냅 → 상한 속도(340) — ✅ e68f4a4 (2026-08-10)
- 실플레이 피드백 배치: 노드맵 막4/5 반전 공개(9→15 컴팩트·바이올렛) + 만렙 오버플로 보상 + 회수 문서 가림/BGM 폴백 회귀 수정 — ✅ 9c5e1f1 (2026-08-10)
- 변전소 후반 압박 보강(sniper 3020·drone 2000) — ✅ 6d1af0b (2026-08-10)
- 막4/5 정체성 분화: act_identity §6·§7 + 램프 재분배 + 재머 4맵 확산 + 바이올렛 침식 + core_recovery 시그니처 배경 — ✅ 688966d (2026-07-08)
- 5막 재구조화 골격: 3막(9)→5막(15스테이지), ACTS/TOTAL_STAGES/RUN_VERSION + 맵 재분배 + 엔드게임 막5 이주(core_recovery) — ✅ 53516ec (2026-07-07)
- 라이벌 VEIL §4 시선 거짓: 딴 데 보는 척 기습 patrol(Enemy.feign_ambush + 압축 텔레그래프 각성 + feigns) — ✅ 51b2969 (2026-07-07)
- 연습장 패널 막별 그룹화 + 라이벌 맵 강조(ACTS 파생 + MapData 라이벌 요소 툴팁) — ✅ e39d68a (2026-07-07)
- 라이벌 VEIL §7 SENTINEL reveal: 페이크 보스 처치 후 라이벌 첫 발화+내 VEIL 동요+복선 지직거림(대사 플레이스홀더) — ✅ a7f3132 (2026-07-07)
- 라이벌 VEIL §4 위장 함정: 가시→안전 바닥 위장(DisguisedSpike + _FloorGlitch tell + 밟기/근접 리빌 + deceit_spikes) — ✅ (2026-07-07)
- 라이벌 VEIL ⓐ 막3 확산: datacenter ARENA 최종 웨이브 재머(s6 전투 풀 3맵 전부 라이벌 요소) — ✅ a12cc09 (2026-07-07)
- 라이벌 VEIL §4 거짓 렌더: 위장한 적(Enemy.disguise_as + _GlitchTell + 리빌 + deceits) — ✅ (2026-07-06)
- 라이벌 VEIL §5 1단: 재머 유닛(EnemyType.JAMMER + VeilSight 마커 소등 + 재밍 필드) — ✅ (2026-07-06)
- 웹 백그라운드 탭 오디오 음소거(head_include, 본편+데모 EoY) — ✅ 79c5e10/eff1edf (2026-07-06)
- 부서지는 엄폐 농성 기믹 맵 '저지선'(DestructibleCover+BulletTrap+웨이브 재조합) — ✅ 7754ec7 (2026-07-04)
- 쓸어내는 해저드(리듬) 기믹 맵 '감시 회랑' + SweepBeam 프리미티브 — ✅ 8f4c3d7 (2026-07-04)
- 이스터에그 4종(황금 적·평화주의·코나미 스킨·숨은 로어 문서) — ✅ da5ae6c/eee3d59/2736342/85f7e24 (2026-07-04)
- 디버그 표시등 + 연습장 새 맵 누락 수정(RouteData 파생 29맵) — ✅ f867e9b (2026-07-04)
- 모바일: 폰 디버그 잠금해제(코너 7탭) · 설정창 반응형(닫기 잘림) · 입력 시 자동 전체화면 토글 — ✅ f5fb0e9/12fc5cb (2026-07-03)
- 코요테 후 지상점프 소모 + 외벽 옥상 저격 둥지 +20 상향 — ✅ 1971e20 (2026-07-03)
- 아레나 방어 기믹 맵 '반응로 제어실' + DefenseCore 프리미티브 — ✅ 6d9ecbb (2026-07-03)
- 본편 막3+엔딩 재설계: 회수→처리선택 4종→엔딩 9개(B1·B2·B3, 대사 플레이스홀더) — ✅ 8ad7da9/bb005a5/4f8014b
- 과제 전시용 포스터 v2(FULLY AI-GENERATED 4부문) + 피드백 창구(구글폼 QR/게임 링크) — ✅ e8065f2/0c23339
- 엔진 포스터 v1 업그레이드(엔딩행·코너·썸네일·2배 고해상) — ✅ 16c18a4
- VEIL 어투 아크(진행도→신뢰+사망 재설계) 코드 이식 + 전체 md 최신화 — ✅ 82c2bf9/a2fccbc 외
- 스킬 반영 그래픽(총알 티어별·부착물·fire_boost 총기 외형·획득 플래시) — ✅ 304ce03 외 (실플레이 점검은 A)
- 스킬 선택 시각화(독립 트리 오버레이) — ✅ 6b574fa
- 맵 진행 노드맵 — ✅ 17b206f / 오프닝 VEIL 눈(BriefingVisual) — ✅ 42a2b9c
- RouteMap 맵 카드 적 아이콘 — ✅ 30855a7 / 권장 스킬 칩 + 스포일러 차단 — ✅ (06-15)
- ESC 어디서든 최우선 — ✅ (06-15) / arcturus 잠긴 문 DENIED→GRANTED — ✅ 692ccaa
- 전역 텍스트 아웃라인 — ✅ c306d22 / 비네트 셰이더화 — ✅ 7ffb9a4 / 아이콘 5종 확산 — ✅ 795f5e6
- 관통 중복 정리(활강 T3=유도 / 사격강화 T3=관통) — ✅ (06-15)
- 글라이드 T1 재설계(패시브 활강+삼단점프) + 글라이드 전용 숨은 보상 게이트 — ✅ 0837140 / cd1035f 외
- 해상도 조절 설정 — ✅ a00421c
- 발사 함정(BulletTrap/LaserTripwire) + 저격수 둥지 재배치 + 못 잡는 적 안내 — ✅ acecc86 외
- shield T3 "방어막 재충전" 구현 — ✅ (06-09)
- FAMILY_COLORS 단일화 — ✅ 4abf394
- 문서 정리: 세션 로그 아카이브 통합 — ✅ b72b616 / outdated docs archive 이동 / STORY 인벤토리 재동기화 / barrier desc 대조 — ✅
