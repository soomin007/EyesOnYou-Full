# 전투 밸런스 프레임워크 (2026-08-17 신설)

> 사용자: "오연사+유도+피해+관통+사거리면 멀리서 대충 갈기기만 해도 다 녹는다.
> 밸런스를 어떤 기준으로 해야 할지 모르겠다." · 기준을 세우고, 봇 계측으로 수치화한다.

## 1. 진단 · 왜 무너지나

스킬이 서로 **곱연산**으로 쌓인다: 발수(오연사 ×5) × 발당 피해(사격강화) × 대상 수(관통)
× 명중률(유도 → 사실상 1.0) × 교전 거리(사거리 → 피탄 위험 0). 개별로는 멀쩡한 강화가
곱해지면 전투의 세 가지 비용을 전부 면제한다:
- **조준 비용** → 유도가 면제
- **접근 위험** → 사거리가 면제
- **대상 선택** → 관통·다발이 면제

비용이 다 사라지면 남는 건 "버튼 꾹" = 사용자가 말한 "대충 갈기기".

## 2. 기준 4 (이 게임의 밸런스 원칙)

1. **강화는 출력을 올리되, 면제를 주지 않는다.** 면제류 스킬(유도·관통·장사거리)은
   반드시 반대급부를 갖는다(발당 피해 감쇠, 관통 시 다음 대상 감쇠 등).
2. **곱연산 차단.** 면제류끼리는 곱해지지 않게 설계한다(예: 유도는 중앙탄 1발에만,
   오연사와 유도의 곱을 끊는 방법).
3. **표준 조우 시간 하한.** 기준 조우(순찰 2+방패 1)를 만렙 빌드가 처리하는 시간이
   기본 빌드의 40% 밑으로 내려가면 초과 화력. 보스는 시간 하한을 룰로 보장
   (P3 창당 피해 상한이 선례: 출력과 무관하게 최소 교전 수를 강제).
4. **수치는 실측으로 검증.** 감이 아니라 봇 스위트([BOT] 지표)로 빌드별
   클리어 시간·받은 피해·발사 수를 재고, 목표 밴드를 벗어나면 조정.

## 3. 봇 계측(사용자 "사람 같은 플레이 테스트 모델" 요청)

- `scripts/BotDriver.gd`: 휴리스틱 봇. Input 액션을 실제로 눌러 Player의 진짜 코드
  경로로 플레이. "대충 하는 사람" 근사: 골 방향 전진, 막히면 점프, 같은 높이 적에게
  반응 지연(0.25s) 후 꾹 사격. 실력 = 파라미터(반응·교전 거리·조준 높이).
- `scripts/BotRunner.gd` + `scenes/bot_runner.tscn`: 빌드 프리셋(base/mid/max) × 맵
  스위트를 time_scale 3으로 자동 주파, `[BOT]` 한 줄 지표 출력.
  실행: `godot --path . --audio-driver Dummy scenes/bot_runner.tscn`
- `MapData._bot_bench()`(route_bot_bench): 봇 계측 전용 표준 조우 벤치 · 게임 미노출
  (RouteData 미등록이라 선택지에 안 뜬다). 평지 ARENA에 동일 조우(순찰 2+폭탄병 1)
  3웨이브. 수직 지형은 봇 등반 한계가 측정을 오염시켜(datacenter 교훈) 평지로 고정,
  방패병은 상성 카운터(폭발물) 요구라 화력 비교에서 제외.
- 봇의 빌드별 습관 재현: 유도 빌드(glide T3)는 교전 중 주기 점프(유도가 공중 사격
  한정이라 그 빌드 유저는 뛰면서 갈긴다). 아레나에선 전방향 조준 + 공중 적만 남으면
  점프 사격.
- 한계(정직하게): 학습 모델이 아니라 손맛·재미·회피 스킬은 못 잰다. 잘 재는 것 =
  "이 빌드로 대충 갈겨도 되는가"(시간·피탄·처치), 지금 문제의 핵심 지표와 정확히 일치.
  봇이 대충 갈기는 플레이 그 자체이므로, 봇 성적이 좋을수록 그 빌드는 무비용이다.
- **v2 · 사용자 근사(2026-08-18 "봇을 좀 더 나같이", §9)**: 실플레이에서 습관 지표를
  자동 수집(`GameState` 플레이 습관 프로필 → `user://playstyle.cfg`, 스테이지 단위 EMA)해
  봇이 소비한다. 수집: 분당 대시/점프/수류탄 · 공중 발사 비율 · 발사 시 최근접 적 거리 ·
  처치/조우 비율(전멸 성향) · 분당 피격(대조용). 봇 반영: **사냥 모드**(통과형 맵에서도
  근처 적 소탕 · 사용자 실측 런당 처치 133 반영) · 이동 리듬 대시 + 날아오는 탄 회피 대시 ·
  교전 중 점프 사격 리듬 · 교전 거리 · 수류탄(표적 교착 시). 데드락 가드 3중(사격 표적
  6s · 사냥 목적지 7s · 허공 갈기기 8s 무진전 포기, 킬 나면 리셋). 표본 0이면 사용자의
  알려진 스타일 근사 기본값. 남은 한계: 회피 정밀도는 사용자보다 낮고(dmg_pm 실측 대조로
  후속 튜닝 가능), 높은 둥지 등반은 여전히 불완전(변전소 8/13).

## 4. 조정 후보 3안 (사용자 결정 대기 · 실측 후 적용)

- **A안(추천) · 곱 차단**: 유도는 중앙탄 1발에만 적용(오연사×유도 해체) + 관통은
  관통마다 다음 대상 피해 50% 감쇠. 각 스킬의 정체성(유도=한 놈은 확실히 / 관통=줄
  세우면 이득)은 유지하고 곱만 끊는다. 변경 폭 최소.
- **B안 · 면제에 세금**: 유도탄은 발당 피해 절반(약탄) · 관통탄은 속도 느림(회피 여지).
  전 발수 유지라 손맛 변화 적으나 "유도 = 약해도 편함"의 하위 선택지화 위험.
- **C안 · 배타 선택**: 오연사 T3와 유도 T3를 같은 트리 분기(둘 중 하나)로. 빌드
  다양성은 최고, 스킬 트리 구조 변경이라 공사 최대.

목표 밴드(초안 · 실측으로 보정): max 빌드 표준 맵 클리어 시간 ≥ base의 45%,
받은 피해는 base보다 낮되 0에 수렴하지 않을 것.

## 5. 계측 이력

- 2026-08-17 1차: base/mid/max × 변전소·창고·철거. max에 glide(유도)가 빠져 있어
  유도를 못 잰 스위트 · 통과형 3맵에서 빌드 간 차이 15% 이내라는 것만 확인.
- 2026-08-18 최종(유도 포함 max + 표준 조우 벤치, 시간=게임초 / dmg=받은 피해):

| 맵 | base | mid | max(오연사+피해+관통+사거리+유도) |
|---|---|---|---|
| 표준 조우 벤치(전멸 3웨이브) | 12.1~17.7s · dmg 1~2 | 10.5s · dmg 1 | **7.5s · dmg 0** (2런 동일) |
| 창고(통과·근접전) | 28.3s · dmg 3 | 28.1s · dmg 5 | **19.1s · dmg 1** |
| 철거 구역(통과) | 21.4s · dmg 4 | 18.0s · dmg 1 | 14.9s · dmg 2 |
| 변전소(통과·원거리) | 16.3s · dmg 4 | 14.7s · dmg 1 | 14.2s · dmg 1 |
| 데이터센터(전멸·수직) | 참고치: 전 빌드 미완주(봇 등반 한계, 상층 저격·드론 도달 불가) |

해석:
- **전투가 강제되는 곳에서 붕괴가 수치로 확인**: 벤치에서 max는 base의 42~62%
  (평균 ~50%, 목표 밴드 45% 경계~이탈) + **받은 피해 0**(2런 모두) = 피탄 위험 소멸.
- **유도가 결정타**: 유도 없는 max(1차)는 창고 27.7s였는데 유도를 넣자 19.1s.
  공중에서 갈긴 유도탄이 방패병 정면 교착까지 부순다(조준·접근 비용 동시 면제 실증).
- mid(오연사 T1+피해 T1)는 base 대비 -12~-29%에 그치고 피해도 받는다 = 건강한 성장.
  문제는 max 조합의 곱연산이라는 진단 그대로.

## 6. A안 적용 (2026-08-18 사용자 결정)

- 유도(glide T3): 전 발 → **공중 사격 중 중앙탄 1발만**(부채꼴 각 0 탄) 유도+데미지 +1.
  `Player._spawn_bullet`의 `idx * 2 == total - 1` 조건. 오연사×유도 곱 해체.
- 관통(fire_boost T3): 관통마다 다음 대상 피해 절반(최소 1, ceil). `Bullet._on_body_entered`.
  부수 발견: 스킬 설명은 "1체 추가 관통"인데 실제는 무한 관통이었다 · 설명을 실동작
  ("뚫을 때마다 피해 감소")으로 동기화(SkillTreeData + ui.md + growth_system.md).
- 적용 후 재계측: base 4맵은 변화 없음 확인(15.9/28.4/21.2/12.1s · A안은 base 무영향이
  맞다). mid/max 재계측은 §7(2026-08-19 완주)로.

## 7. A안 재계측 완주 (2026-08-18 세션2)

datacenter를 스위트에서 제외(수직 지형 봇 데드락 · 전멸형 대표는 벤치가 맡음)하고
전 빌드 × 4맵 완주 + 벤치만 3회 반복으로 분산 확인.

| 맵 | base | mid | max(A안 이전 → 이후) |
|---|---|---|---|
| 벤치(4런) | 12.2~18.2s · dmg 1~3 | 10.6~10.7s · dmg 1 | 7.5 → **7.3~7.5s · dmg 0**(4런 전부) |
| 창고 | 28.5s · dmg 3 | 28.1s · dmg 5 | 19.1 → 20.1s · dmg 1 |
| 철거 구역 | 21.0s · dmg 2 | 18.1s · dmg 2 | 14.9 → 14.7s · dmg 2 |
| 변전소 | 16.0s · dmg 3 | 14.7s · dmg 2 | 14.2 → 14.1s · dmg 1 |

판정:
- **시간 하한(기준 3)**: 벤치 max/base = 41~60%(base 분산 12.2~18.2가 큼 · 평균 ~45%).
  40% 하한은 넘지만 여유 없는 경계선.
- **피탄 0 탈출: 실패** · 벤치 max dmg 0이 4런 전부. 다만 실전 3맵(창고·철거·변전소)에선
  max도 dmg 1~2를 받는다.
- **핵심 발견: A안은 벤치에 사실상 무효**(7.5 → 7.3~7.5 · 오차 범위). 이유가 실측으로
  설명된다 · 벤치 적(순찰·폭탄병)은 저HP라 관통 감쇠(최소 1)로도 원킬이고, 평지 접근형이라
  유도 없이도 부채꼴이 다 맞는다. **벤치 붕괴의 주범은 오연사 부채꼴 × 사격강화 자체**,
  유도·관통이 아니다. A안이 유의미하게 먹힌 곳은 창고(방패병 근접전, +5%)뿐.
- 관통 감쇠율을 더 조여도 벤치엔 무의미(저HP 원킬)라 backlog의 "미달이면 감쇠율 조정"
  경로는 실측상 막힘. 선택지 3안(수용/오연사 반대급부/밴드 재정의)을 제시했고
  **사용자 결정 = 제4안 "적을 강화해"** → §8.

## 8. 적 강화 · 막 진행 스케일 (2026-08-18 세션2, 사용자 "적을 강화해")

스킬을 더 깎는 대신 적을 강화한다. 후반 막 한정이라 "막3까지는 데모 느낌" 제약과
엘리트 램프(막4+) 경계에 정합. 서사 = 라이벌 침식(라이벌의 군대와 같은 축).

구현(`Enemy.gd`):
- **내구(HP)**: 막4 순찰 2→3 · 방패 3→4. 막5 순찰 4 · 방패 5 · 저격 1→2 · 드론 1→2.
  폭탄병 1 유지(원거리 1샷 정답 보존, 엘리트와 동형) · 재머 제외(라이벌의 "손"은 별개 문법).
  엘리트 절대값(6/7/3/3)이 뒤에서 덮어 계급 우위 유지.
- **사격 빈도**: `_pressure_interval_mul()` = min(risk3 0.7, 막4 0.85, 막5 0.75).
  risk와 막 배수는 **곱하지 않고 min 결합**(기준 2 곱연산 차단은 적 쪽도 동일 ·
  ELITE_SNIPER_INTERVAL "회피 가능선" 주석의 0.7 하한 보존). 예고(텔레그래프) 시간은
  안 건드림 — 빈도만 조이고 공정성 유지(elite_enemies_plan §0.2와 동형).

검증(막5 벤치 `route_bot_bench_late` 신설 · stage 12 · 실전형 조우 = 순찰 2+저격 1,
웨이브별 폭탄병/후방 저격 협공 추가 · elite_chance 0 잠금 · 3런 분산):

| 빌드 | 막5 벤치(s12) | 참고: 무균 벤치(s3) |
|---|---|---|
| base | 21.0~25.3s · dmg 9~10 | 12.0s · dmg 1 |
| mid | 17.1~17.3s · dmg 6 | 10.7s · dmg 1 |
| max | **10.1~10.2s · dmg 3**(3런 고정) | 7.5s · dmg 0 |

판정:
- **피탄 0 탈출 달성**: 만렙이 실런에서 실제로 만나는 조우(막5)에서 dmg 3 고정.
  기울기 건강(강할수록 빠르고 덜 맞지만 공짜 아님). max/base(s12) = 43~49%.
- 무균 벤치(s3) max dmg 0은 잔존하나 그 조합(만렙이 stage 3)은 실런에 없다 —
  s3 벤치는 빌드 화력 비교용, s12 벤치가 "공짜로 이기는가" 판정용으로 역할 분리.
- 회귀: 기존 4행(변전소·창고·철거·s3 벤치) 전 빌드 무변화 확인.
- 계측 발견 2건: ① 순찰·폭탄병만으론 어떤 스탯 강화로도 만렙 피탄이 안 생긴다(첫 공격
  사이클 전 전멸) — 피탄은 원거리 위협(저격 협공)이 만든다. ② 봇은 공중(드론)·정면 교착
  (방패병)을 못 풀어 스위트 데드락 — 벤치 구성에서 제외(known_issues 등재).

**실플레이 대기**: 막4~5 체감(순찰 3~4대가 스펀지로 느껴지는지 · 사격 빈도 압박 톤) +
A안 손맛(유도 중앙탄 1발 · 관통 감쇠).

## 9. 봇 v2 기준선 (2026-08-18 세션2 · "봇을 좀 더 나같이")

§3 v2 항목의 구현을 반영한 새 기준선. **사냥 모드 때문에 통과형 맵 수치가 v1과 비교
불가**(처치·시간·피탄 전부 상승) — 이후 계측은 이 표와 비교할 것. 프로필은 계측 로그
첫 줄(`[BOT] profile src=...`)에 남는다(src=default = 실플레이 표본 0).

| 맵 | base | mid | max |
|---|---|---|---|
| 변전소(통과·13기) | 48.6s · dmg 12 · 8/13 | 51.4s · dmg 9 · 8/13 | 29.0s · dmg 3 · 8/13 |
| 창고(통과·4기) | 24.1s · dmg 2 · 3/4 | 23.1s · dmg 1 · 3/4 | 20.3s · dmg 1 · **4/4** |
| 철거(통과·3기) | 21.4s · dmg 4 · 2/3 | 20.8s · dmg 5 · 2/3 | 15.6s · dmg 2 · **3/3** |
| 무균 벤치 s3 | 12.2s · dmg 1 | 10.7s · dmg 1 | 7.9s · dmg 0 |
| 막5 벤치 s12 | 21.7s · dmg 9 | 17.0s · dmg 6 | 10.4s · dmg 2 |

- 사냥 성향이 사용자와 유사해짐(처치 2~4배 · 창고/철거는 max 전멸). 변전소 잔여 5기는
  봇 등반 한계(높은 둥지) — 정직한 한계로 유지.
- 데드락 0(변전소도 포기 가드로 완주). 기록 경로 스모크: 봇 3런이 playstyle.cfg에
  EMA 누적되고 자기일관 확인(지시 dash_pm 4.0 → 실측 4.04 · 처치율 = hunt_ratio 0.86),
  검증 후 파일 삭제(봇 습관이 사용자 프로필을 오염하지 않게 · 수집은 실플레이만).
- **다음 캘리브레이션**: 사용자가 실플레이하면 프로필이 쌓인다(src=user) → 스위트
  재실행으로 봇이 실측 습관을 입는다. 회피 정밀도는 dmg_pm 실측 대조로 조정 여지.

## 10. 계측 인프라 교훈 (2026-08-18 세션2 후반 · 방 체인 계측하며 발견)

- **time_scale 3 물리 양자화가 계측을 오염**: scale만 올리면 물리 delta 0.05로 거칠어져
  명중률·회피가 실플레이와 다르게 측정된다(변전소 발사 98→187 실측). 해법 =
  `physics_ticks_per_second`를 배속만큼 올려(180) 게임초당 delta 1/60 보존.
- **경과 시간은 "실측 × 현재 scale"이 아니라 게임초 타이머 폴링 누적으로**: 게임 내
  슬로모·연출·Player._exit_tree 안전망이 scale을 바꾸는 순간 전체 구간이 왜곡된다.
- **봇 레버 관문 3연 함정**(순서대로 발견): ① 등반 시퀀스가 1단 상승 중 높이 게이트가
  꺼지며 자가 리셋(더블점프 입력 유실) ② 지면→레버 발판 Δ220은 더블점프(190) 물리 한계
  밖 — 레버 발판은 Δ160 이하로 ③ 레버 감지 Area 반폭 20px — 봇 정지 문턱 14px로.
- **봇은 해저드 대기 리듬을 건너뛴다**(HP 리필이라 증기·낙수 강행): 체인 맵 봇 시간은
  실플레이어보다 20~40% 짧게 나온다. 절대 밴드 판정은 [RUN] 실측이 최종, 봇은
  완주 가능성 + 상대 비교 + 체류 배수용.
- 스위트 기준선(§9)은 이 개정으로 다시 무효 — v3 기준선은 room_chain_expansion §3
  배치 1 표를 볼 것(비체인 회귀 행 포함).
- **2026-08-19 추가(배치 2 계측하며)**: ① 게임 pause는 골 Area 판정을 죽인다 — 러너는
  레벨업(level 99 + XP 0 리셋)·도감 카드(seen 사전 등록에 **jammer 포함**)를 반드시 차단
  ② "최근접" 표적 선택은 무리 지은 적에서 프레임마다 스왑 → 교착 타이머가 영구 리셋 —
  **표적 고정**(죽거나 포기까지 유지)이 필수 ③ 봇 기법 이식 v2.1(사용자 직접 설명):
  대공 점프샷 · 방패병 크로스(2회 제한) · 2회 포기 = 영구 무시. 명중 정밀화(정점 발사
  타이밍·크로스 성공률)는 후속 ④ 방당 타임아웃 120s(체인 방 확대 반영 · 실패 판정이
  아니라 측정 상한) ⑤ 스크린샷 창모드 실행도 반드시 `--audio-driver Dummy`(무음).

## 11. 기본 연사 하향 + 사격 감속 (2026-08-20 사용자 확정)

실플레이 라운드 1의 사용자 판정: "공격 버튼에서 손을 떼는 게 손해일 정도로 무지성 공격이
너무 사기" + "주인공 무기는 권총이지 자동소총이 아니다". 확정 방향 = 감속과 하향 **둘 다**.

구현(`Player.gd`):
- **ATTACK_COOLDOWN 0.30 → 0.42**(3.3발/s → 2.4발/s). T2 속사(×0.70)면 0.294s로
  예전 기본과 비슷 — "속사를 찍으면 옛 손맛"이라는 성장 서사가 됨.
- **사격 감속**: 발사마다 0.45s 감속 창 갱신(쿨다운보다 살짝 길어 홀드 중 연속) ·
  이동 ×0.7. 손을 떼면 곧 풀 스피드 = 쏘는 타이밍이 위치 선정 판단이 된다.
  대시 분기가 우선이라 대시 회피는 감속 무관(탈출 수단 보존).
- **fire_boost T2 재설계**: 옛 "사격 후 0.5s 가속 ×1.4"는 새 감속과 정면 모순(홀드 중
  오히려 가속)이라 제거 → "감속 완화(×0.7 → ×0.85)"로 교체. desc = "쏘면서도 덜 느려짐".

**계측 주의**: §5~§9의 모든 표는 이 변경 *이전*(연사 0.30 · 감속 없음) 기준이다.
봇도 사격하며 이동하므로 통과·전멸 시간이 전반 상승한다 — 다음 스위트 실행 때
새 기준선을 뜨고, XP 경제 조임 캘리브레이션(08-20 [RUN] 실측)과 같은 세션에서 처리.

## 12. 대시 무적 창 축소 (2026-08-21 사용자 확정)

배경: "전반적으로 맞을 일이 없다"(hp_survival_economy §1) — 피격 압력 1단계 논의 중
대시 항목만 사용자 선결정("무적 창 전체 축소" 선택 · 연속 감쇠안·현행 유지안 기각).

종전: 대시 전 구간 무적(0.18s) + dash_boost T3 추가 0.3s = T3 기준 무적 0.48s /
쿨다운 0.56s(T1 -20%) → **무적 가동률 86%**. 연타만으로 ~500px/s 무적 이동이 성립,
어떤 사선 압력도 무력화되는 구조적 상한이었다.

구현(`Player.gd` `_try_dash`):
- 무적 = 대시 **앞부분만 0.12s**(뒤 0.06s는 취약 · 이동 거리/속도/쿨다운 불변).
- dash_boost T3 추가 무적 0.3 → **0.15s**(합계 0.27s · T3 가동률 ~48%).
- 스킬 설명 동기("대시 후 0.15s 무적"). 손맛 지표: 단발 회피는 종전과 거의 동일,
  "누르고만 있으면 안 맞는" 연타 커버리지만 절반으로.

후속: 압력 1단계(사선 교차·밀도) 적용 후 실측으로 추가 폭 판정. 48%도 높으면
연속 대시 감쇠(2연속째 무적 없음)를 2차 후보로.

## 13. 새 기준선 (2026-08-21 · 연사 하향 §11 + 대시 무적 §12 + 저격 재조준 + 압력 파일럿 반영)

전체 스위트(10맵 × 3빌드) 무타임아웃 완주. §5~§9 표는 전부 구식 — 이후 비교는 이 표 기준.
봇 프로필 src=user(stages 120). 형식: time(s) / dmg / kills.

| 맵 | base | mid | max |
|---|---|---|---|
| substation s4 (파일럿) | 111.8 / 18 / 24 | 104.8 / 12 / 24 | 73.3 / 9 / 26 |
| warehouse s3 | 98.5 / 10 / 24 | 94.8 / 7 / 25 | 58.5 / 3 / 27 |
| cooling s4 | 79.8 / 11 / 9 | 69.0 / 11 / 10 | 46.3 / 6 / 13 |
| control_corridor s6 | 96.3 / 22 / 23 | 80.0 / 16 / 23 | 80.3 / 13 / 22 |
| server_hall s6 | 139.8 / 46 / 19 | 104.3 / 21 / 20 | 61.5 / 11 / 22 |
| relay_station s10 | 125.0 / 15 / 20 | 120.0 / 12 / 21 | 67.3 / 8 / 25 |
| demolition_zone s1 | 37.0 / 12 / 4 | 42.3 / 5 / 4 | 34.3 / 4 / 5 |
| pump_station s1 (파일럿) | 95.0 / 2 / 7 | 89.0 / 0 / 8 | 88.0 / 0 / 8 |
| bench s3 | 12.8 / 0 | 11.3 / 0 | 8.5 / 0 |
| bench_late s12 | 26.0 / 14 | 18.8 / 8 | 11.0 / 4 |

읽기:
- **피탄 0 붕괴가 사실상 끝났다** — 구기준선의 max 피탄 0 행이 일반 맵에서 사라짐(펌프장
  mid/max 0은 막1 완만 설계 의도). 재조준+밀도가 지표에 실린다는 실증.
- 빌드 기울기 전 행 건강(base > mid > max). 시간은 연사 하향 몫만큼 전반 +10~20%.
- **server_hall base 46 관찰 대상**: 방3(재머 방) 53.5s에서 집중 피격 — 봇이 재머 존에서
  마커 없이 오래 버티는 습성 몫이 있어 실플레이 판정 필요. 과하면 그 방 저격부터.
- 감시탑은 스위트 제외(봇 수직 내비 부재) · 실플레이 전용.
- XP 경제 캘리브레이션은 이 기준선 + 실플레이 [RUN] 확보 후(§11 예고대로).
