# 막3 보스(lab SENTINEL) 리워크 (§8 전면 리워크 구현 완료 · 실플레이 판정 대기)

> 2026-08-21 사용자 진단: "패턴도 너무 단순하고 재미 요소도 없고, 쓸데없이 반복만 하다가
> 끝난다. 스토리가 느껴지지도 않고, 도전도 참신한 기믹도 없다. 연출도 시시해서 중간보스로도
> 안 느껴진다. 원래 '최종 보스인 척'하는 배경을 생각하면 더더욱." 확대 패스(배기·위장 자폭,
> 08-19)로는 부족 판정. **이 문서가 리워크의 단일 소스** · 방향 승인 후 별도 세션에서 구현.

## 1. 무엇이 실패했나 (진단의 구조화)

- **루프가 하나뿐**: "빨간 신호 멎은 틈에 쏘기"가 처음부터 끝까지 유일한 문법. 페이즈가
  바뀌어도 룰이 안 바뀐다(명명 연출 규칙 위반: 보스 = 룰 변화여야 한다).
- **무대가 안 변한다**: 14-1은 복층 무대·격벽·스윕으로 공간 자체가 전개되는데, lab은
  평평한 ARENA에 포탑형 보스 하나. "중간보스 체급"조차 공간이 안 만들어 준다.
- **서사가 전투 안에 없다**: "이것이 최종 방어선이다"라는 연극(페이크 최종 보스)이 대사
  한 줄로만 존재하고, 전투 자체는 그 연극을 연기하지 않는다.
- **연출 스케일**: 등장 경보 3줄 + 스폰. 14-1의 인트로/무대 대비 왜소.

## 2. 컨셉 재정의 · "시설이 연기하는 최종 보스"

SENTINEL은 진짜 최종 병기가 아니라 **시설(그리고 그 뒤의 라이벌)이 무대에 올린 연극**이다.
이 설정을 전투 룰로 번역한다: SENTINEL의 힘 = 시설 그 자체. 페이즈마다 **막1~2에서 배운
시설 기믹이 보스의 무기로 소환**된다 · "여태 지나온 시설 전체가 이 병기의 몸이었다"는
서사가 룰 변화로 체감되고, 반복이 학습의 회수로 바뀐다.

## 3. 리워크 골격 (3페이즈 · 룰이 바뀌는 보스)

- **인트로(연출 격상)**: 경보 시퀀스 확장 · 조명 소등 → 격납 셔터 개방(배경 변형) →
  거대 실루엣 기동 순차 점등 → 현 3줄 경보. 카메라 인/줌 아웃 1회. BGM 보스 트랙 인.
  무대 = 현 ARENA를 세로로 확장(상부 정비 데크 2단 신설 · 14-1 복층 문법의 전조).
- **P1 · 위용(현행 계승 + 학습)**: 현 패턴(신호-틈 사격) 유지하되 짧게. 포대가 대칭
  전개되는 개막 연출. "이건 배운 문법"이라 빨리 지나간다(현 20초 체감을 P1 하나로).
- **P2 · 시설 소환(룰 변화 1 · 참신성의 핵)**: 코어가 격납 링을 열고 **시설 기믹을
  2가지 소환**: 바닥 증기 분출(냉각) + 방전 아크(변전). 소환 순간마다 배경 셔터가 열리며
  해당 설비가 무대에 실물로 등장 · 보스 공격과 환경 리듬을 동시에 읽어야 한다.
  상부 정비 데크가 회피 고지(단 드론 증원이 데크를 견제). 노드 HP 핍 유지.
- **P3 · 과부하 폭주(룰 변화 2 + 기존 자산 회수)**: 과부하 배기(쏠수록 배출 가속)를
  P3 전용으로 이동 + 낙하 잔해(철거) 소환 + 위장 자폭 → 재점화(현행 유지 · 이 연극의
  절정으로 재배치). 재점화 후 30~40초의 진짜 발악 = 전 기믹 동시 가동(짧고 굵게).
- **격파 = 서사 회수**: 무너지는 연출을 "너무 순순히 무너진다"로 연출(코어가 저항 없이
  셧다운 · 조명이 위에서부터 순차 소등). 직후 라이벌 첫 발화(기존 reveal)가 그 위화감을
  회수 · "이제야 그 연극의 관객이 누구였는지 안다"가 전투 경험과 일치하게 된다.

## 4. 구현 스코프 추정

- 무대 확장 + 인트로 연출: Stage lab ARENA 개정 + 배경(격납 셔터·실루엣) 1식.
- P2 기믹 소환: SteamVent·ElectricArc 재사용(소환 타이밍 배선만 신규) + 데크 2단.
- P3: 기존 배기·위장 자폭 재배치 + FallingDebris 재사용.
- BossSentinel.gd 페이즈 룰 개정 + 텔레그래프 유지(예고 시간 불변 원칙).
- 세션 1~2회 규모. 하니스: 페이즈 전이·소환 무결성 + 스크린샷 + 봇 실탄 교전 시간.

## 5. 결정 포인트 (2026-08-22 사용자 확정)

1. P2 소환 기믹 = **증기+방전(추천안)** · "일단 추천대로 해보고 별로면 그때 또 고민".
2. 목표 전투 시간 = **현 최종 보스전 수준(60~90s)** · "지금의 최종 보스전 시간이 중간
   보스에서 걸리고, **최종 보스전(14-1)은 그보다 더 길게**"(→ §7 후속).
3. 데크 = 견제 고지(무응답 → 추천안 적용 · 기존 P2 드론 소환이 이 역할).
4. 재도전 인트로 단축 = 확정(기존 boss_intro_seen_run 가드가 이미 충족 · 재도전 시
   인트로 전체 생략 + 음악 킥 점프).

## 6. 1차 구현 (2026-08-22 · 같은 날)

- **인트로 무대 연출**: 소등(0.5) → 격납 셔터 두 판 개방(1.2 · hatch 사운드+흔들림) →
  조명 3개 순차 점등(실루엣 비춤) → 어둠 걷힘 + 기존 경보 비트. 전부 스킵 가능 ·
  총합이 음악 킥(25s)과 정합하게 기존 비트 대기 재배분.
- **P2 시설 소환**(`Stage._summon_facility_hazards` · deferred · flushing 함정 준수):
  바닥 해치 플래시 4곳 + SteamVent 2기(x480/1440 · h200) + ElectricArc 2구간
  (620~820 · 1100~1300 · 위상 교대) + 경보 "격납 개방" + VEIL 학습 회수 자막 1회.
  보스 격파 시 즉시 정리(페이드 트윈은 격파 프레임 완주 불보장 · 스모크 실측).
- **HP 재조정**: 28→36(P2 25 · P3 13 · 자폭 7). 창당 상한이 max_hp 비례라 배기 창 수
  불변 · 창 사이 교전 + P2 회피 부하로 연장.
- **격파 소등**: 조명 3밴드 순차 소등(옅은 어둠이 reveal까지 잔류) · "순순한 셧다운"의
  위화감을 라이벌 첫 발화가 회수.
- 검증: 런타임 스모크 5항목 PASS(스폰·인트로 종료·P2 소환 4노드·정리·HP) + 스크린샷 2장.
- **보류**: 무대 자체 확장(ARENA FIXED 카메라 = 월드 확대 시 화면 축소 · 카메라 재작업
  필요라 별도 검토) · P3 배기 전용화(전 페이즈 시간 하한 역할이 커서 유지).

## 7. 후속 · 14-1을 SENTINEL보다 길게 (사용자 확정 · 미착수)

SENTINEL 실측(내일 실플레이)이 60~90s에 안착하면 14-1 목표 = 그 위(90~120s+).
후보 지렛대: P3 실체화 창 수 8 → 10~12 · P2 노드 재접속 1→2회 · 창당 상한 분모 8→9.
실측 후 폭 결정.

## 8. 전면 리워크 (2026-08-25 사용자 C안 확정 · 당일 구현)

5차 갤러리 지적 3건 회수: ⓐ "드론형 보스라 플레이어는 상층 발판에 있는데 바닥 함정이
무의미" ⓑ "체력이 적어 과열 패턴만 잦고 귀찮다" ⓒ "보스전 자체가 재미없다".

- **무대 세로 확장**: lab ARENA 1920x900(FIXED) → **2200x1300(ARENA_FOLLOW)** · §6에서
  보류했던 카메라 문제를 14-1 복층 문법(기존 ARENA_FOLLOW 모드) 이식으로 해소.
  수직 사다리 = 지면 1220 → 스텝 1096 → 중층 ~950 → 상층 ~810 → **정비 데크 1단 668
  (좌/우 렉 + 연결 플레이트) → 데크 2단 528(중앙 꼭대기)**. 시그니처 배경 신설(격납고:
  상부 격납 셔터 + 캣워크 레일 + 데크 경고등 + 분출구 주의 띠) · 구 등간격 수직 격자 폐지.
- **ⓐ 고도 추적 + 시설 재배치**: P2+ 호버가 플레이어 고도-260 밴드로 내려온다(400~980
  클램프) · 상층 캠핑이면 눈앞까지 온다. 방전 아크 2구간은 지면 → **데크 1단 위**(캠핑
  비용이 캠핑 자리에), 증기 분출구는 지면 유지 + plume 620(= 유인 실속의 "내려가면 보상").
  데크 2단은 아크 없음 · 보스 경합·스윕이 대신 다툰다. 비행체 콜리전 mask 0(발판 관통 ·
  자폭 착지는 수동 클램프).
- **ⓒ 데크 스윕(신규 앵커 패턴)**: P2+ 주기(7.5s/P3 5.2s)로 플레이어 고도를 예고(0.8s ·
  날개 주황 점멸 + 무대 관통 경로선) 후 수평 관통 돌진(640px/s · 접촉 1뎀). "어느 발판이든
  스윕 한 번이면 비켜야 한다". 배기/실속/자폭/전환이 스윕을 취소.
- **ⓑ 배기 리듬 완화**: HP 36→52(P2 36 · P3 19 · 자폭 8) + 창당 상한 분모 5.5→**3.8**
  (배기 ~5회→**~3회**) + 배기 3.0→2.6s. 창 하나가 길어 사격 리듬이 덜 끊긴다.
- 검증: 런타임 스모크 7항목 PASS(스폰·P2 시설 4노드(아크 y668)·스윕 상태기·P3 잔해·
  창 상한 14·자폭·위장 재점화 hp10) + 스크린샷 2장(무대/추적 · 스윕 예고).
- **실플레이 판정 대기**: 전투 시간 60~90s 안착 여부 · 스윕 예고 가독/빈도 · 고도 추적의
  압박 톤(짜증 vs 긴장) · 데크 아크 배치 체감 · 유인 실속 사용률.
