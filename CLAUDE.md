# Eyes on You — 프로젝트 운영 규칙

## 대사 검수 루틴 (2026-08-22 신설 — 위반 시 커밋 금지)

**플레이어에게 노출되는 한국어 문구(대사·자막·브리핑·패치 노트)를 새로 쓰거나 고친 커밋은,
커밋 전에 반드시 "무맥락 대사 검수"를 통과해야 한다.** 배경: 대사 품질 지적이 8회 이상 반복.
원인은 규칙(known_issues·메모리)이 세션 시작에만 읽히고 작성 시점에 재소환되지 않는 것,
그리고 작성자 자신의 맥락으로 자기 검수를 하면 지시 대상 누락을 못 본다는 것.
`.claude/settings.json`의 PostToolUse 훅(`tools/hooks/dialogue_watch.py`)이 .gd 편집에서
한국어 문자열을 감지해 이 루틴을 상기시킨다.

**절차**: 커밋 직전, 서브에이전트(Task, 맥락 없는 새 에이전트)에게 다음만 준다:
1. 신규/수정된 줄 전체(한국어 + EN)
2. 각 줄이 나오는 순간 **화면에 실제로 그려진 것 목록** (렌더 코드 기준으로 작성자가 뽑는다)
3. 화자와 상황 한 줄 (예: "전술 AI VEIL이 요원에게 무전")

서브에이전트는 줄마다 PASS/FAIL 판정한다. 체크리스트:
- ① 지시 대상이 화면 목록에 실제로 있는가 (없는 사물·개념 지칭 = FAIL)
- ② 필수 성분 생략 없는가 — 설정을 모르는 첫 플레이어가 이 줄만 듣고 이해하는가
- ③ 더빙 게임에서 실제로 들리는 발화인가 (문어체 조합·번역투 관용구·억지 은유 = FAIL)
- ④ 화자에 맞는가 (VEIL = 전술 AI의 보고체 합쇼체, 요원 상대. 연애·동행 톤 금지.
  warm 밴드의 따뜻함은 전우애·직업 언어로만)
- ⑤ 종결어미 다양한가, "-어요" 연속 2줄 금지, "..." 비트당 1회 이하
- ⑥ 설계 용어·어려운 말 없는가 (격벽·소거·실체화 창 같은 내부 용어 금지, 쉬운 일상어로)

FAIL이 하나라도 있으면 고치고 재검. 검수 결과(N줄 PASS)는 커밋 메시지 본문에 한 줄 남긴다.
상세 규약: `docs/design/dialogue_review.md`, 메모리 korean-dialogue-register.

## 세션 시작 루틴 (가장 먼저)

매 세션을 시작할 때, 본격 작업에 들어가기 전에 다음을 먼저 읽고 **"지금 어디까지 왔고 다음 후보가 무엇인지"** 방향을 사용자에게 제안한다:

1. **`docs/design/backlog.md`** — 합의됐으나 미착수인 작업 목록 (다음 작업의 단일 소스)
2. **최신 `session_logs/YYYY-MM-DD.md`** — 직전 세션의 상태·결정·미해결
3. **`docs/design/known_issues.md`** — 반복하지 말아야 할 함정/오류
4. `git log --oneline -10` + `git status` — 누적 변경

→ 이를 종합해 "직전까지 한 것 / 다음 후보 X·Y·Z / 추천 한 가지"를 한눈에 정리해 제시한 뒤 사용자 선택을 받고 시작한다. 사용자가 곧장 작업을 지시하면 그것부터 하되, 위 파일들로 맥락을 먼저 맞춘다.

## Git commit / push 루틴 (2026-07-06 개정)

**커밋은 작게 자주. 사용자가 테스트해야 하는 코드/기능은 바로 push, 문서류만 모아서 push.**
GitHub Actions 워크플로가 push마다 실패해 알림 메일이 쌓이므로(2026-07-06 사용자 지시), *불필요한*
push만 줄인다. 테스트가 필요한 변경까지 참으면 사용자가 확인을 못 한다 — 그건 참지 않는다.

- 매 세션 시작 시 `git status` + `git log --oneline -5`로 누적된 변경 확인
- 의미 있는 작업 단위(기능 추가, 시스템 변경, 버그 수정 등)가 끝나면 끊어서 **commit**
- **테스트 대상 코드/기능 변경은 완료·검증 후 바로 push.** 사용자가 실플레이/실기기로 확인해야 하므로.
- **문서만 바뀌는 커밋(세션 로그·backlog·known_issues·설계 문서·README)은 모아둔다** — 테스트 대상이
  아니고 push마다 실패 워크플로 메일이 오므로. 다음 코드 push 때 함께 올리거나 사용자가 요청할 때.
- commit 메시지: 한국어, `prefix(scope): 설명` 형식 (feat/fix/refactor/docs 등). 이모지 없음. 본문은 변경 항목 bullet
- main 직접 push (이 프로젝트는 branch/PR 흐름 안 씀)
- destructive 작업(force push, reset --hard 등)은 별도 확인

한 세션에 31개 파일이 한 번에 누적된 적 있으니(2026-05-02) **커밋 자체는 계속 작게 끊는다** — 바뀐 건
문서-only push의 빈도뿐. 근본 해결은 실패하는 워크플로를 고치는 것(필요 시 별도 요청).

## 오류 기록 루틴 (반복 방지)

세션 중 버그·설계 함정·작업 실수를 발견하면 **`docs/design/known_issues.md`** 에 "증상 → 원인 → 재발 방지책" 형태로 기록한다. 세션 종료 시 그 세션에서 나온 오류를 정리해 추가한다.

목적은 같은 실수를 두 번 겪지 않는 것 — 세션 시작 루틴에서 이 파일을 먼저 읽으므로 자연히 예방된다. 게임 버그뿐 아니라 작업 프로세스 실수(잘못된 도구 호출, 커밋 누락 등)도 포함한다.
